<div class="info-item">
    <label for={{ name }} class="name-field">{{ label }}</label>
    <input class="field-value input" name={{ name }} 
            type={{ type }} value={{ value }}
            placeholder={{ placeholder }} {{ readonly }}>
</div>
